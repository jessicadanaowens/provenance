= javascript_include_tag "welcome"
= javascript_include_tag "sign_up_forms"

#welcome
  .first.section{align: "center"}
    .container

      %h1 Manage your art collection online for free.
      %button.pink-outline-button.static
        %a{href: "#sign-up", class: 'anchorLink'} Try it for free

  .second.section{align: "center"}
    .container
      %h2 Document the provenance of each unique piece over a lifetime.

  .third.section{align: "center"}
    .container
      %h2 Easily export your documents and share with others.

  .forth.section{align: "center"}
    .container
      %h2 Connect with other collectors and artists.

  .section#sign-up
    .welcome-login
      .content
        .outter-form
          .inner-form
            %h5.login-title
              Get Started Absoluteley
              %span
                FREE
            %p.small no credit card needed

            = form_for(@user, html: {id: 'welcome-page-sign-up'}) do |f|
              = render 'shared/error_messages'

              %input(style="display:none;" type="text" name="somefakename")
              %input(style="display:none;" type="password" name="anotherfakename")

              .field.name
                = f.label 'Name', class: 'name'
                = f.text_field :name, autocomplete: 'off', class: 'name'
                .error.name

              .field.email
                = f.label 'Email', class: 'email'
                = f.email_field :email, class: 'email'
                .error.email

              .field.password
                = f.label 'Create Password', class: 'password'
                = f.password_field :password, class: 'password'
                .error.password

              .field.submit
                = f.submit "sign up free", class: 'submit pink-button'
                %p.small-text
                  By clicking "Sign Up Free" I agree to Provenance's
                  %a{href: "#"}
                    Terms of Service

                .welcome-login-link
                  %p.small-text
                    Already have an account?
                    %a{href: login_path} Login!
